#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>
#include <string.h>
#include <stdio.h>

int _putchar(char c);
int _printf(const char *format, ...);
int handle_see(const char *format, va_list agrs);


#endif
